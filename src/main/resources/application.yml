server:
#  port: ${SERVER_PORT:8080}
  port: 8081

spring:
  datasource:
#    url: ${DATABASE_URL:jdbc:postgresql://localhost:5432/vrp_ai?currentSchema=public}
#    username: ${USERNAME}
#    password: ${PASSWORD}
    url: jdbc:postgresql://103.82.132.72:5432/postgres?currentSchema=final
    username: postgres
    password: postgres123
    driver-class-name: org.postgresql.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
#        default_schema: final
    show-sql: true
  elasticsearch:
    uris: 103.82.132.72:9200
